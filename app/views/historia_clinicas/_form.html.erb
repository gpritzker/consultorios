<%= form_for(@historia_clinica) do |f| %>
  <% if @historia_clinica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historia_clinica.errors.count, "error") %> prohibited this historia_clinica from being saved:</h2>

      <ul>
      <% @historia_clinica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profesional_id %><br>
    <%= f.number_field :profesional_id %>
  </div>
  <div class="field">
    <%= f.label :practica_id %><br>
    <%= f.number_field :practica_id %>
  </div>
  <div class="field">
    <%= f.label :especialidad_id %><br>
    <%= f.number_field :especialidad_id %>
  </div>
  <div class="field">
    <%= f.label :paciente_id %><br>
    <%= f.number_field :paciente_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
